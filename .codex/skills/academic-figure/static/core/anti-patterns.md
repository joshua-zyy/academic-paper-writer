# 图表反模式（Figure Anti-Patterns）

| 模式 | 问题 | 正确做法 |
|---|---|---|
| 美观优先 | 使用 rainbow/jet 色板让图“好看” | 使用灰度安全、克制的学术色板 |
| 无 QA 交付 | 代码跑通就直接交付 | 必须执行 QA Contract：可读性、数据一致性、格式合规 |
| 虚构架构 | SVG 或 prompt 包含不存在的模块连接 | 架构只能来自代码/论文证据；未确认项必须标记 |
| 复杂架构硬画 SVG | 用 Python boxes/arrows 勉强绘制模型框架图，导致信息拥挤且视觉质量不足 | 默认使用 `architecture-image` 生图模型工作流，必要时叠加可编辑短标签 |
| 风格词堆砌 | prompt 只写“顶会风、专业、精美”，没有阅读路径、布局分区、元素指令和信息密度控制 | 先写 Visual Director Brief，再生成结构化 prompt |
| 硬编码路径 | 文件路径写死为开发者本地路径 | 使用相对路径或参数化配置 |
| 只交付 prompt | 用户要求绘图时只给外部生图提示词 | 数据图交付 SVG；架构图交付生图结果或明确 blocker；prompt 仅限显式请求或降级说明 |
