# Evidence Inventory Schema

本文件为指针文件。权威版本（source of truth）位于 `skills/shared/schemas/evidence-inventory.md`。

编排器和所有消费者应读取 `skills/shared/schemas/` 路径。

如需修改 schema，请编辑 `skills/shared/schemas/evidence-inventory.md`。
